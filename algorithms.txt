booking model: 
    listing
    checkIn
    checkOut
    owner
    status : request sent
             request granted
             request rejected
             paid
             resumed
             confirmed
             rejected by guest
             accepted by guest
             completed

Add booking algorithm:
- if listing.isInstantBooking
    goto "confirm and pay" page
    add booking and set status = paid
  else:
    goto "Send book request" page
    add booking and set status = request sent